# Safe VLA（CBF-FM）— 复现方案

> **等级：C→仿真 B（机制）**  
> **审查卡：** `Safe VLA：在 Flow Matching 内嵌 CBF 安全约束_26.08.04_卡.md`  
> **项目页：** https://safe-vla-fmcbf.netlify.app/  
> **匿名仓：** https://anonymous.4open.science/r/diffusion_cbf-1B55/README.md（本机访问被 Cloudflare 拦截，机制自实现）

## 1. 目标与口径

**目标：** 复现核心机制——在 Flow Matching 去噪后段用 CBF-QP 修正速度场，使整段 action chunk 满足安全约束；并与无过滤、后置 E2E 投影对照违规率。

**成功标准：**

1. 匿名仓能 clone **或**机制可自实现（本仓库采用后者并标明原因）。
2. 2D 环境给出无过滤 vs CBF 修正的违规率对照。
3. `notes/vs-acc-cbf.md` 写明「生成内修正 vs 动作后置投影」。

**明确不做：** 微调 π₀、接 SO-101、把项目页 25–30 Hz / 100% 真机安全率写进本机结果表。

## 2. 锁死设定

| 项 | 值 |
|----|----|
| 滤波启动 | 仅 \(\tau \ge 0.6\) |
| 聚合 | Log-Sum-Exp soft-min over chunk×obstacles |
| QP | \(\min \|\delta\|^2\) s.t. \(\nabla H\cdot(v+\delta)+\alpha H \ge 0\) |
| 对照方法 | `none` / `e2e_cbf`（后置） / `cbf_fm`（生成内） |
| 场景 | `default` / `tight` / `aggressive` 三组数据 |

## 3. 核心接口

```python
def barrier_correct_velocity(v, x, tau, tau_on: float = 0.6, ...):
    """tau < tau_on 时原样返回 v；否则对 v 做满足 CBF 的最小扰动。"""
```

单元测试：不安全名义速度在 \(\tau \ge 0.6\) 后应使 CBF 不等式成立（或违规计数下降）。

## 4. 最小复现路径

```text
pip install -r requirements.txt
python -m pytest tests/ -q
python scripts/run_experiments.py
```

## 5. 验收清单

- [x] README / 复现方案 / vs-acc-cbf 文档齐全
- [x] `src/cbf_fm_step.py` 实现后段滤波接口
- [x] 2D Maze 无过滤 vs CBF-FM（及后置 E2E）违规率对照
- [x] 三组不同数据完整日志
- [x] 合同/机制测试通过
- [x] 匿名仓不可用时标明降级路径

## 6. 相对审查卡的更新

审查卡建议先下匿名仓再接 Maze。本机对 `anonymous.4open.science` 的请求被 bot 验证页拦截，故按项目页与审查卡公式自实现 2D 积分器 + LSE-CBF-QP。机制结论可迁移；绝对值不可与项目页 Maze/真机表直接对比。
